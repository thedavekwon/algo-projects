#include <cmath>
#include <iostream>
#include <vector>

using namespace std;

void update(vector<var> &tree, int node, int start, int end, int index, var diff) {
    
    
}


int main() {
    int N;
    cin>>N;
    vector<int> nums(N);
    for(int i=0;i<N;i++) {
        cin>>nums[i];
    }
    
}
